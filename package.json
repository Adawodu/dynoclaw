{
  "name": "dynoclaw",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "convex:dev": "npx convex dev",
    "convex:deploy": "npx convex deploy",
    "dashboard:dev": "npm run dev --workspace=@dynoclaw/dashboard",
    "dashboard:build": "npm run build --workspace=@dynoclaw/dashboard"
  },
  "dependencies": {
    "convex": "^1.21.0"
  }
}
